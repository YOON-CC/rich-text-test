# 🌈 랜덤 마크다운 샘플

> “잡동사니라도 정리하면 문서가 된다.” — *어느 개발자*

## 1) 개요

- 프로젝트명: **Nebula-β**
- 버전: `v0.3.7`
- 상태: ⚙️ 진행 중 / ⏳ 리뷰 대기
- 태그: #문서 #테스트 #마크다운 #RTF #랜더링

---

## 2) 체크리스트

- [x] 제목/목차 구성
- [x] 표(Table) 포함
- [x] 코드블록 (JS/TS)
- [x] 수식(인라인/블록)
- [x] 각주/각주참조[^1]
- [ ] 다크모드 대비 컬러 점검

---

## 3) 표 (정렬, 코드, 이모지 혼합)

| 구분 | 설명 | 우선순위 | 메모 |
|:---:|:-----|---------:|:-----|
| A   | 초기 설정(Init) | 1 | `npm run dev` |
| B   | API 연동 🔗 | 2 | 엔드포인트: `/v1/items` |
| C   | UI 컴포넌트 🎨 | 3 | 버튼/모달/툴팁 |
| D   | 국제화(i18n) 🌍 | 2 | ko-KR / en-US / ar-SA |
| E   | 접근성 ♿ | 1 | 대비비 4.5:1 이상 |

> 참고: 마크다운 표는 **셀 병합이 안 됨**.

---

## 4) 코드 블록

### 4.1 JavaScript

```js
export const greet = (name = '세상') => {
  const msg = `안녕하세요, ${name}!`
  console.log(msg)
  return msg
}

greet('Chan')
```

### 4.2 TypeScript

```ts
interface RoadmapItem {
  id: string
  title: string
  priority: 1 | 2 | 3
  completed: boolean
}

const addItem = (items: RoadmapItem[], item: RoadmapItem) => [...items, item]

addItem([], {
  id: 'nebula-beta',
  title: 'RTF 변환기',
  priority: 1,
  completed: false,
})
```

---

## 5) 수식 (인라인/블록)

인라인 수식: $E = mc^2$ 는 질량-에너지 등가식을 뜻합니다.

블록 수식:

$$
  f(x) = \int_{-\infty}^{\infty} \hat{f}(\xi) e^{2 \pi i x \xi} \, d\xi
$$

---

## 6) 각주

본문에 [각주를 달면](#) 텍스트 끝에 각주가 표시됩니다.[^2]

[^1]: 각주는 문서 하단에 위치합니다.
[^2]: 복수의 각주도 지원합니다.

# 🌈 랜덤 마크다운 샘플

> “잡동사니라도 정리하면 문서가 된다.” — *어느 개발자*

## 1) 개요

- 프로젝트명: **Nebula-β**
- 버전: `v0.3.7`
- 상태: ⚙️ 진행 중 / ⏳ 리뷰 대기
- 태그: #문서 #테스트 #마크다운 #RTF #랜더링

---

## 2) 체크리스트

- [x] 제목/목차 구성
- [x] 표(Table) 포함
- [x] 코드블록 (JS/TS)
- [x] 수식(인라인/블록)
- [x] 각주/각주참조[^1]
- [ ] 다크모드 대비 컬러 점검

---

## 3) 표 (정렬, 코드, 이모지 혼합)

| 구분 | 설명 | 우선순위 | 메모 |
|:---:|:-----|---------:|:-----|
| A   | 초기 설정(Init) | 1 | `npm run dev` |
| B   | API 연동 🔗 | 2 | 엔드포인트: `/v1/items` |
| C   | UI 컴포넌트 🎨 | 3 | 버튼/모달/툴팁 |
| D   | 국제화(i18n) 🌍 | 2 | ko-KR / en-US / ar-SA |
| E   | 접근성 ♿ | 1 | 대비비 4.5:1 이상 |

> 참고: 마크다운 표는 **셀 병합이 안 됨**.

---

## 4) 코드 블록

### 4.1 JavaScript

```js
export const greet = (name = '세상') => {
  const msg = `안녕하세요, ${name}!`
  console.log(msg)
  return msg
}

greet('Chan')
```

### 4.2 TypeScript

```ts
type Status = 'pending' | 'in_review' | 'done'

interface ChecklistItem {
  id: string
  title: string
  done: boolean
}

const toggle = (list: ChecklistItem[], targetId: string): ChecklistItem[] =>
  list.map((item) => (item.id === targetId ? { ...item, done: !item.done } : item))
```

---

## 5) 수식

- 인라인: $e^{i\pi} + 1 = 0$
- 블록: $$\int_{0}^{\infty} e^{-x^2} \, dx = \frac{\sqrt{\pi}}{2}$$

---

## 6) 각주

[^1]: 각주는 변환 시 위치에 따라 본문 하단에 배치됩니다.

